import { OneTimeDirective } from './on-time.directive';

describe('OneTimeDirective', () => {
  it('should create an instance', () => {
    const directive = new OneTimeDirective();
    expect(directive).toBeTruthy();
  });
});
